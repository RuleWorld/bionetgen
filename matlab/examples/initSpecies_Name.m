function [species_init] = initSpecies_Name( params )

    % THE FOLLOWING DATA SHOULD BE WRITTEN BY A WRITE M-FILE METHOD

    

    species_init = zeros(1,10);
    species_init(1) = params(1);
    species_init(2) = params(2);
    species_init(3) = params(3);
    species_init(4) = params(4);
    species_init(5) = 0;
    species_init(6) = 0;
    species_init(7) = 0;
    species_init(8) = 0;
    species_init(9) = 0;
    species_init(10) = 0;



end