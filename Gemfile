source "https://rubygems.org"

# Matches the Jekyll + plugin versions GitHub Pages actually builds with.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x no longer ships webrick in stdlib; needed for `jekyll serve`.
gem "webrick"
